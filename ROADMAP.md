# 로드맵

**작성 기준일: 2026-08-27**

---

# ★ 확정된 개발 계획 (2026-08-27 승인)

사용자가 제품 스펙 27개 항목을 제시했고, 전수 대조 후 **Phase 1·2만 진행하기로 확정**했습니다.
Phase 3·4는 스펙에는 있으나 **이번 범위 밖**입니다.

진행 상황은 [TASKS.md](TASKS.md)의 P1-*/P2-* 항목에서 체크박스로 추적합니다.

## Phase 1 — 새 데이터 소스 0, DB 스키마 변경 0

| # | 작업 | 왜 이 순서인가 |
|---|---|---|
| P1-1 | **Bull / Bear Case (규칙 기반)** | `criteria` 배열에 항목별 점수·실제값·임계값이 이미 전부 있음. 새 계산이 없어 가장 안전 |
| P1-2 | **Peer 비교 지표 확장** | ROIC·성장률·FCF마진 등 전부 `scores.json`에 존재. 표에 열 추가 수준 |
| P1-3 | **Fair Value 다중 방법 + 가중평균** | Reverse DCF·Historical P/E·P/FCF·EV/EBITDA 추가 후 가중 결합 |
| P1-4 | **Custom Strategy Builder** | 지표 조건식(`ROIC>15% AND D/E<0.5`) + 전략 저장 |

## Phase 2 — Supabase 스키마 추가 (여전히 무료 티어)

| # | 작업 |
|---|---|
| P2-5 | **Portfolio Tracking** — 보유수량·평균단가 저장, 평가금액·비중·섹터노출 |
| P2-6 | **Thesis Breaker 사용자 정의** — 조건 저장 + 마이페이지 감시 |
| P2-7 | **Alert 임계값 사용자 설정** — `alerts.ts` 하드코딩 값을 사용자 값으로 |

## 이번 범위에서 **의도적으로 뺀 것** (이유 포함)

되살리려면 아래 제약 중 하나를 먼저 풀어야 합니다.

| 스펙 항목 | 뺀 이유 |
|---|---|
| **AI Investment Report · AI가 쓰는 Bull/Bear** | LLM API는 전부 유료 → "유료 API 금지" 제약과 충돌. **규칙 기반으로 대체**(P1-1) |
| **Analyst Revision · Earnings Surprise · Analyst Consensus** | 애널리스트 추정치에 무료·무키 소스가 없음. Yahoo `quoteSummary`는 crumb 인증이 걸린 비공식 엔드포인트라 상시 파손 위험 |
| **이메일 / 푸시 알림** | Resend 무료 티어 + Actions 크론으로 0원 구현은 가능하나 **새 인프라**. 우선순위 낮음 |
| **결제 / 유료 티어** | Stripe 연동 자체는 무료지만 **Vercel Hobby는 상업적 이용 금지** → 수익화 시 Pro($20/월) 강제. 기술이 아닌 사업 결정 |
| **백테스트 Survivorship Bias 제거** | `universe.json`이 *오늘자* S&P 500이라 과거 구간이 낙관 편향. 과거 시점 구성종목은 유료 데이터. **제거 불가 — 화면 고지가 최선** |

> Look-ahead bias는 이미 구조적으로 차단돼 있습니다 (`lib/backtest.ts:11-28`). Survivorship bias와 혼동하지 마세요.

---

# 그 밖의 관찰 항목

> **이 아래 문서의 성격에 대한 경고**
>
> 아래 항목은 **과거 세션의 계획을 복원한 것이 아닙니다.** 그런 기록은 저장소에 없습니다.
> 전부 **현재 코드/주석에서 "지금은 안 한다"고 명시한 지점**을 모은 것이고, 출처를 각 항목에 달았습니다.
> 출처가 `[코드 근거]`인 것은 코드가 스스로 미래를 언급한 것이고,
> `[분석 제안]`인 것은 이번 분석에서 새로 제기한 것입니다.

## 현재 막혀 있는 것 — 없음

2026-08-27 기준 배포와 로그인 모두 정상 동작합니다.
(직전까지 막혀 있던 Vercel 배포 실패와 Supabase 인증 리다이렉트는 둘 다 해결 — [TASKS.md](TASKS.md) T1·T2)

---

## 단기 — 이미 있는 것을 정확하게

### R1. 누락 5종목 처리
`APA` `EQR` `HONA` `SYF` `TFC`가 유니버스에 있으나 점수가 없습니다 (503 → 498).
README 34줄이 4종목의 원인(은행 특유 매출항목 부재 / 분할 신설법인)을 이미 진단해 두었고,
`EQR`은 그 목록에 없어 원인 미확인입니다.
`lib/xbrl.ts`의 `CIK_OVERRIDES` 또는 `lib/sec.ts`의 태그 목록을 넓히면 일부는 살릴 수 있습니다.
**출처: [코드 근거] README:34 + 데이터 실측**

### R2. 코드-문서 불일치 정리
- `types.ts`의 `Fmp*` 이름 → 실제 소스(SEC+Yahoo)를 반영한 이름으로 (순수 리네이밍, 동작 무변화)
- `useTheme.ts` — 존재하지 않는 토글을 설명하는 주석 정리 또는 파일 정리
**출처: [분석 제안]**

### R3. 섹터 평균 PER의 근거 개선
현재 11개 섹터의 하드코딩 근사치입니다. 코드 주석이 "정밀한 지수가 아니라 sanity check"라고
스스로 한계를 인정하고 있습니다. `scores.json`에 이미 498종목의 PER이 있으므로
**유니버스 자체에서 섹터 중앙값을 계산**하면 외부 소스 없이 근거가 생깁니다.
**출처: [코드 근거] `lib/scoring.ts:24-27`**

---

## 중기 — 시간이 쌓여야 가능한 것

### R4. 일간 히스토리 기반 정밀 백테스트
현재 백테스트는 분기말 14개 시점만 씁니다. 일간 forward 행 499개는
"분기 주기와 일간 주기를 섞으면 보유기간이 들쭉날쭉해진다"는 이유로 **의도적으로 배제** 중입니다.
코드 주석이 직접 말합니다 — *"일간 forward 히스토리가 몇 달·몇 년 쌓이면 더 세밀한 백테스트가 가능해진다. 오늘은 아직 아니다."*
→ **할 일은 기다리는 것.** 배치가 매일 도는 한 저절로 조건이 갖춰집니다.
**출처: [코드 근거] `lib/backtest.ts:30-38`**

### R5. 종목 상세 페이지 캐싱
방문마다 외부 6곳을 호출합니다 (위키·뉴스·Form4·거래소·주가·히스토리).
성질이 다릅니다 — 뉴스/Form4는 신선도가 생명이라 실시간이 맞지만,
**위키 요약과 거래소명은 거의 변하지 않습니다.** 이 둘만 캐시해도 호출이 1/3 줄어듭니다.
**출처: [분석 제안] — 단, `lib/news.ts:3`이 "뉴스를 배치에 넣지 않는 이유"를 이미 설명하므로 그 판단은 존중할 것**

### R6. 테스트 도입
테스트가 0개입니다. 이 코드베이스는 **순수 함수와 I/O가 이미 깨끗하게 분리**되어 있어
(`scoring.ts` `backtest.ts` `dca.ts` `peers.ts` `alerts.ts` `customWeights.ts`는 네트워크 없이 실행 가능)
테스트를 붙이기에 유리한 상태입니다. 특히 `scoring.ts`는 규칙이 복잡하고 돈이 걸린 판단이라
회귀 위험이 가장 큰 곳입니다.
**출처: [분석 제안] — 단, `lib/backtest.ts:5`가 "네트워크 없이 단위 테스트 가능"을 설계 목표로 명시**

---

## 장기 — 예산이 필요하거나 성격이 바뀌는 것

### R7. 이메일/푸시 알림
`lib/alerts.ts` 첫 줄이 명시합니다 — *"여기엔 이메일도 푸시도 없고, **예산 없이는 앞으로도 없다.**"*
현재는 페이지를 열었을 때 화면에서만 변화를 보여주는 방식으로 우회하고 있습니다.
**무료 스택 제약과 정면으로 충돌하는 항목입니다.**
**출처: [코드 근거] `lib/alerts.ts:3-4`**

### R8. 유니버스 확장 / 자동 갱신
S&P 500 구성종목은 분기마다 바뀌는데 `data/universe.json`은 수동 관리이고 갱신 스크립트가 없습니다.
README 27줄이 "주기적으로 다시 생성해야 한다"고 적어두었습니다.
**출처: [코드 근거] README:27**

### R9. 모바일 앱
README 90줄이 Supabase를 고른 이유로 "나중에 React Native 등에서 같은 프로젝트의
REST API·SDK로 로그인/데이터를 공유할 수 있다"를 듭니다. **선택의 근거로 언급된 것이지 계획은 아닙니다.**
**출처: [코드 근거] README:90**

---

## 명시적으로 하지 않기로 되어 있는 것

이건 미래 항목이 아니라 **지켜야 할 제약**입니다. 되돌리려면 의식적인 결정이 필요합니다.

| 제약 | 근거 |
|---|---|
| **유료 API 도입 금지** — 전 구간 무료·키 불필요 스택 | README:6, `lib/sec.ts:2` |
| **파생상품 거래(옵션·RSU) 미표시** — 보상 이벤트라 노이즈 | `lib/insiderTrading.ts:26-32` |
| **라이트 테마 없음** — 디자인 핸드오프에 스펙이 없어 추측하지 않음 | `app/globals.css:3-8` |
| **일간 히스토리에 criteria 상세 미저장** — 연 180MB 중복 | `003_score_history_detail.sql` |
| **의존성 최소 유지** | `lib/insiderTrading.ts:17-24` |
| **카카오 로그인 불가** — 비즈니스 인증 필요 (기술 문제 아님) | README:107 |
